import { createStyles } from "@mantine/core";

export const useStyles = createStyles((theme) => ({
  wrapper: {
    display: "flex",
    justifyContent: "center",
    marginTop: 40,
    marginBottom: 40,
  },
  form: {
    width: "70%",
  },
}));
